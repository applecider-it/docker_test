name: docker_wsl

services:
  node:
    image: node:22-alpine
    user: "1000:1000"
    working_dir: /app
    ports:
    - "4173:4173"
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run dev"
    tty: true
    container_name: node
